This file serves as a template (example) for required environment variables.

DO NOT commit your actual .env file with real secrets to Git.

--- SERVER CONFIGURATION ---

SERVER_URL=http://localhost:3001
PORT=3001
NODE_ENV=production

--- DATABASE ---

DATABASE_URL=mongodb://<USER>:<PASSWORD>@<HOST>:<PORT>/<DATABASE_NAME>

--- AUTHENTICATION (JWT) ---

JWT_SECRET=<SUPER_LONG_SECRET_KEY>
JWT_REFRESH_SECRET=<SUPER_LONG_SECRET_KEY>
JWT_ACCESS_TOKEN_MAX_AGE=<TIME_IN_SECONDS>    # e.g., 3600 (1 hour)
JWT_REFRESH_TOKEN_MAX_AGE=<TIME_IN_SECONDS>  # e.g., 86400 (24 hours)

--- PAYMENT INTEGRATION (Stripe) ---

STRIPE_PRIVATE_KEY=<STRIPE_SK_KEY>
STRIPE_SECRET_KEY=<STRIPE_SK_KEY> # Often the same as PRIVATE_KEY, but listed for clarity
STRIPE_WEBHOOK_SECRET=<STRIPE_WHSEC_KEY>

--- THIRD-PARTY AUTH (OAuth) ---

Note: These are example IDs and secrets for development

GOOGLE_CLIENT_ID=<YOUR_GOOGLE_CLIENT_ID>
GOOGLE_CLIENT_SECRET=<YOUR_GOOGLE_CLIENT_SECRET>
FACEBOOK_CLIENT_ID=<YOUR_FACEBOOK_CLIENT_ID>
FACEBOOK_CLIENT_SECRET=<YOUR_FACEBOOK_CLIENT_SECRET>